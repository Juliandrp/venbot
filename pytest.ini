[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    slow: marca tests lentos (omitir con -m "not slow")
    integration: tests que tocan servicios externos (mockear o skip)
